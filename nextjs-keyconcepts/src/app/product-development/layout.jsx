import { createPageMetadata } from '@/lib/seo'

export const metadata = createPageMetadata('product-development')

export default function ProductDevelopmentLayout({ children }) {
  return children
}
