{
  "name": "kcipl",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "yarn --cwd nextjs-keyconcepts dev",
    "build": "yarn --cwd nextjs-keyconcepts build",
    "start": "yarn --cwd nextjs-keyconcepts start",
    "lint": "yarn --cwd nextjs-keyconcepts lint",
    "dev:api": "yarn --cwd backend dev",
    "install:web": "yarn --cwd nextjs-keyconcepts install",
    "install:api": "yarn --cwd backend install"
  },
  "packageManager": "yarn@1.22.22"
}
