import { createPageMetadata } from '@/lib/seo'

export const metadata = createPageMetadata('younited-communities')

export default function YounitedCommunitiesLayout({ children }) {
  return children
}
