import { createPageMetadata } from '@/lib/seo'

export const metadata = createPageMetadata('election-mobilization')

export default function ElectionMobilizationLayout({ children }) {
  return children
}
