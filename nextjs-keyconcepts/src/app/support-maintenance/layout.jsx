import { createPageMetadata } from '@/lib/seo'

export const metadata = createPageMetadata('support-maintenance')

export default function SupportMaintenanceLayout({ children }) {
  return children
}
